namespace: widget-service

public:
  - web:3000

services:
  web:
    image: node:4
    volumes:
      - .:/app
    working_dir: /app
    command: [npm, start]
    depends_on:
      - mysql
  mysql:
    image: mysql

dependencies:
  - service-registry
  - testMicrosvc
  - feature-flags
