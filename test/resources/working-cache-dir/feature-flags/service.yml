namespace: feature-flags

public:
  - web

services:
  web:
    image: node:4
    ports:
      - "1337:1337"
    volumes:
      - .:/app
    working_dir: /app
    command: [npm, start]
    depends_on:
      - dynamo
  dynamo:
    image: dynamo

dependents:
  - service-registry
